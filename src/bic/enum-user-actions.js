define(function () {
  'use strict';

  /**
  Enum for user action types on a Blink Form Action.
  @readonly
  @enum {number}
  */
  var USER_ACTION = {
    SAVE: 0,
    SUBMIT: 1,
    DISCARD: 2
  };

  return USER_ACTION;
});
