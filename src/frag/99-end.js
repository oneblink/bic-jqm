define('bic/model/application', ['bic/main'], function (bic) {
  'use strict';
  return bic;
});

define('bic', ['bic/main'], function (bic) {
  'use strict';
  return bic;
});

window.require(['bic']);
